# Phase 3.1 Plan: Intelligent Prompt Refinement Update

> **Objective**: Update the core `systemInstruction` in the API service to utilize an advanced, multi-step prompt engineering structured instruction provided by the user. This guarantees higher quality reframing operations.

## Steps
1. **Update `api.js` System Instruction**:
   - Replace the legacy constructed meta-prompt with the comprehensive, 6-step framework requested by the user.
   - Retain the `Improvement Mode` logic by conditionally appending format/constraint modifiers (`Quick` vs `Expert`) at the end of the new prompt, or omitting them if the new prompt covers all bases natively. The user's prompt covers most grounds natively, so we will append the mode specific rules as an additional constraint block.
2. **Rebuild the project**: Compile the changes into `dist/` using the build scripts.
3. **Update GSD status docs**: Update `STATE.md` to reflect Phase 3 completion.

## Expected Outcome
When a user runs the reframer, the Gemini 2.5 Flash model will receive a much superior `systemInstruction`, yielding higher-quality, rigorously structured prompts.
