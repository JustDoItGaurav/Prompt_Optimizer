# Plan 1.3 Summary
- Built `src/main-ui.js` which spins up a shadow-DOM-like modal containing the loader and the interactive result replacing the prompt natively.
- Developed `src/index.js` as the controller module loading settings and triggering UI updates.
- Wrote `scripts/build.js` utilizing `esbuild` effectively bundling and minifying the whole payload down into a valid javascript IIFE string placed in `dist/bookmarklet.js`.
